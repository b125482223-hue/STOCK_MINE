# MVP 封版紀錄

封版日期：2026-07-10

## 已完成

- GitHub Pages 靜態儀表板與手機版表格介面。
- 上市與上櫃三大法人買賣超資料。
- 法人買賣金額歷史表與台指期三大法人未平倉。
- 每日信用交易變化與自動累積的歷史列。
- 交易日採錯峰多次重試：11:47–17:17、14:50，以及 18:17–22:47。
- 官方尚未發布當日資料或休市時，保留前一交易日網站資料。
- 頁面顯示初步盤後、法人與期貨已更新、信用交易已補齊三種資料狀態。

## 安全與部署

- GitHub Pages 已部署並可正常存取。
- 儲存庫為公開，沒有協作者。
- `main` 規則集已啟用：禁止強制推送與刪除分支。
- Actions 僅允許 GitHub 官方 Actions；更新工作流程只使用 `actions/checkout@v4` 與 `actions/setup-node@v4`。

## 驗證

- `node tests\normalizers.test.js` 通過。
- 資料 JSON 契約檢查通過。
- 手動觸發 `Update market data` 成功。
- GitHub Pages 建置與部署成功。

## 已知限制

- GitHub Actions 排程沒有準時執行保證，實際可能延遲數小時。
- 新日期的整體融資維持率沒有可用的同份官方公開統計，因此顯示 `--`。
- 法人買賣金額是以買賣超股數乘收盤價估算。
- 外部官方 API 若調整欄位或服務，更新工作流程可能失敗；網站會維持最後成功資料。

後續變更應以新功能或資料來源修正為獨立工作，不直接重構本 MVP 的既有資料契約。

## 2026-07-11 顯示調整

- 摘要卡改為外資今日買賣超金額、融資餘額、外資未平倉。
- 摘要副標顯示昨日外資金額與前次增減。
- 漲跌色彩採台股慣例：紅漲、綠跌。
- 未平倉表移除沒有資料的多方、空方欄位。
- 手機摘要卡改為單欄排列；資料 JSON 契約不變。

## 2026-07-13 排程可靠度修正

- 保留 14:50 首次抓取，下午改為每 10 分鐘重試至 17:00。
- 晚間信用交易改為每 10 分鐘重試至 22:30。
- 同一更新工作流程依序執行，避免延遲工作同時推送。
- Actions 執行摘要顯示實際資料日期、更新階段與來源問題。

## 2026-07-13 獨立更新與稽核

- 信用交易不再等待 T86，可在融資融券發布後獨立更新。
- 儀表板顯示信用資料日期、成功更新時間與最後檢查時間。
- 新增 GitHub Actions 手動更新入口，不在公開網頁儲存 Token。
- `data/logs/api-call-history.json` 保留最近 100 次 API 呼叫時間與可用狀態。
- `data/latest/market-dashboard.json` 僅新增 metadata，既有顯示契約不變。

## 2026-07-13 大盤方向

- 沿用 TWSE `MI_INDEX`，不新增外部 API。
- 摘要區新增加權指數卡，顯示收盤指數、漲跌點與漲跌幅。
- 漲跌顏色沿用台股慣例：紅漲、綠跌。
- 指數更新時間獨立記錄於 `sectionUpdates.index`。

## 2026-07-14 排程延遲緩解

- 確認 GitHub Actions 當日下午未依原定時間觸發，並非證交所 API 或頁面快取問題。
- 保留 14:50 正式抓取，另於 11:47–17:17 每 30 分鐘錯峰排隊，吸收數小時排程延遲。
- 晚間信用交易改為 18:17–22:47 每 30 分鐘錯峰排隊與重試。
- API 歷史新增 cron 字串，供後續比對原定排程與實際執行時間。
- 此調整只能降低 GitHub 免費排程漏抓風險；若要求準點 SLA，需改用外部排程器或後端服務。

## 2026-07-14 上市外資金額口徑修正

- 摘要卡的外資買賣超金額限定 TWSE 上市市場，不再納入 TPEx 上櫃市場。
- 新增 `institutionalHistory[].twseForeignTotal`，既有上市加上櫃的 `foreignTotal` 契約維持不變。
- 摘要卡當日與昨日比較都使用相同的上市市場口徑。
- 首頁與 API 紀錄頁的 JavaScript 加入部署版本，JSON 請求加入時間參數，避免 GitHub Pages 或瀏覽器沿用舊資源。

## 2026-07-14 最終封存

- 公開網站：`https://b125482223-hue.github.io/STOCK_MINE/`。
- 實際 Chrome 手機尺寸驗收通過，首頁顯示上市外資 `-512.00 億`，JavaScript 無執行錯誤。
- 最新盤後資料日期為 2026/07/14；API 紀錄、信用交易、法人、期貨與指數採獨立更新。
- GitHub Pages 與 Actions 均已部署；`main` 禁止強制推送與刪除規則維持啟用。
- TPEx 偶發 HTTP 520、TWSE 偶發 HTTP 307 或非 JSON 回應仍屬外部來源風險；失敗區塊沿用最後成功資料。
- 下次接手先查看 `data/logs/api-call-history.json`、Actions 最新執行紀錄與 `sectionUpdates`，再判斷是發布延遲、排程延遲或端點異常。

## 2026-07-15 官方金額與期貨欄位修正

- 法人買賣金額改用 TWSE `BFI82U` 官方彙總買賣差額，不再等待 T86 個股明細或以股數乘收盤價估算。
- 上市法人金額可在官方彙總表發布後獨立更新；大盤與櫃買股數明細仍各自保留最後成功資料。
- TAIFEX 解析器必須確認自營商、投信、外資均具有完整 12 個交易及未平倉數值欄位，才接受未平倉資料。
- 只有交易口數欄位、尚未公布未平倉餘額時，不再把當日多空淨額誤標為未平倉。
- 7/15 正確台股期貨未平倉多空淨額：外資 `-79,557`、投信 `+75,646`、自營商 `-664`、合計 `-4,575`。

## 2026-07-16 排程規則檢討（尚未實作）

- 現行每個交易日共有 23 個名義排程，每次都呼叫全部 9 個 API，理論上最高約 207 次 API 呼叫。
- 2026-07-15 實際觀察到 `14:50` 法人排程於 `17:01` 才執行，晚間信用交易則從 `19:50` 跳到 `22:12`，證實 GitHub Actions 排程仍可能延遲或漏執行。
- 建議下一階段依資料發布時段拆分為收盤價、法人現貨、期貨未平倉、信用交易四種呼叫範圍，預估可降至每日約 48 至 51 次 API 呼叫。
- 待補強項目包括：記錄名義排程時間 `scheduledFor`、跨午夜仍鎖定原交易日、信用資料隔日上午條件式補漏，以及當日資料完成後停止同類重複呼叫。
- 本次只完成檢討與封存，未變更現行 workflow；正式排程仍以 `docs/UPDATE_SCHEDULE.md` 的「現行規則」為準。

## 2026-07-17 頁面自動更新

- 7/17 下午 Actions 在 `14:26` 執行時，法人與期貨尚未發布，後續排程延遲，網站一度沿用 7/16 資料。
- 晚間 `22:53` 的 Actions 已取得 7/17 法人、期貨與信用交易完整資料，公開 JSON 驗證為 `complete`。
- 首頁新增每 5 分鐘重新讀取已部署 JSON，從背景切回頁面時也立即讀取，避免長時間開啟的手機頁面停留在舊資料。
- 自動讀取不會直接呼叫證交所或期交所 API，也不會觸發 GitHub Actions；資料來源仍以部署後的 `market-dashboard.json` 為準。

## 2026-07-20 晚間信用排程修正

- 21:41 直接查詢 TWSE `MI_MARGN` 的 7/20 結果仍為 0 筆，因此當時沿用 7/17 信用資料是正確行為。
- 原定 21:17 的 GitHub Actions 至 21:40 仍無執行或 API 紀錄，確認排程延遲問題持續存在。
- 晚間時段改為 21:35、21:50、22:05、22:20、22:35、22:50、23:05，集中在信用資料預期發布後並增加錯峰重試。
- 本次只修正時段；每次仍呼叫全部 9 個 API。依來源分組、跨日鎖定與條件式停止仍列為後續工作。
